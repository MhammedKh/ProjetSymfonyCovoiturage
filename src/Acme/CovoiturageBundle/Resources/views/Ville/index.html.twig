{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ville list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Nomv</th>
                <th>Altitude</th>
                <th>Longitude</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ville_show', { 'id': entity.id }) }}">{{ entity.nomV }}</a></td>
                <td>{{ entity.altitude }}</td>
                <td>{{ entity.longitude }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ville_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ville_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ville_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
