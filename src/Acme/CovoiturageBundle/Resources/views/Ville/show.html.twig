{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ville</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Nomv</th>
                <td>{{ entity.nomV }}</td>
            </tr>
            <tr>
                <th>Altitude</th>
                <td>{{ entity.altitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ entity.longitude }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ville') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ville_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
